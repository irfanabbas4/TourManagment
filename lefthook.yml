pre-commit:
  parallel: true
  commands:
    fix: 
      run: npm run biome:fix
      exclude: "node_modules"


commit-msg:
  parallel: true
  commands:
    commitlint:
    run: npx commitlint --edit {1}

